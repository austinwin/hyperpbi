import { copyFile, readFile, writeFile } from "node:fs/promises";
import { spawnSync } from "node:child_process";
import { join } from "node:path";

const profile=process.argv[2]==="maps"?"maps":"core";const audit=process.argv.includes("audit");const root=process.cwd();const capabilitiesPath=join(root,"capabilities.json");const buildPath=join(root,"src/providers/providerBuild.ts");const originalCapabilities=await readFile(capabilitiesPath,"utf8");const originalBuild=await readFile(buildPath,"utf8");
try{const capabilities=JSON.parse(originalCapabilities);capabilities.privileges=profile==="maps"?[{name:"WebAccess",essential:false,parameters:["https://tile.openstreetmap.org","https://nominatim.openstreetmap.org"]}]:[];await writeFile(capabilitiesPath,JSON.stringify(capabilities,null,2)+"\n");await writeFile(buildPath,`/** Generated temporarily by package-profile.mjs. */\nexport const EXTERNAL_PROVIDERS_AVAILABLE = ${profile==="maps"};\n`);const flags=[profile==="core"?"--certification-fix":"",audit?"--certification-audit":""].filter(Boolean).join(" ");const result=spawnSync(`npx pbiviz package ${flags}`,{cwd:root,stdio:"inherit",shell:true});if(result.error)console.error(result.error);if(result.status!==0)process.exitCode=result.status??1;else{const dist=join(root,"dist");const files=(await import("node:fs/promises")).readdir(dist);for(const file of await files){if(file.endsWith(".pbiviz")&&!/-(?:core|maps)(?:-|\.pbiviz$)/.test(file)){const source=join(dist,file);const target=join(dist,file.replace(".pbiviz",`-${profile}.pbiviz`));await copyFile(source,target);}}}}finally{await writeFile(capabilitiesPath,originalCapabilities);await writeFile(buildPath,originalBuild);}
