import { ProviderConfiguration } from "./providerTypes";import { resolveProviderPolicy } from "./providerPolicy";export function providerHealth(config?:ProviderConfiguration,webAccessAvailable?:boolean){const policy=resolveProviderPolicy(config,webAccessAvailable);return{status:policy.warnings.length?"warning":"ready",label:policy.certificationSafe?"Core / offline":"External provider mode",warnings:policy.warnings};}
