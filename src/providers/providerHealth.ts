import type {ProviderAccessState,ProviderConfiguration} from "./providerTypes";import{resolveProviderPolicy}from"./providerPolicy";export function providerHealth(config?:ProviderConfiguration,access?:boolean|ProviderAccessState){const policy=resolveProviderPolicy(config,access);return{status:policy.warnings.length?"warning":"ready",label:policy.certificationSafe?"Core / offline":"External provider mode",warnings:policy.warnings};}
