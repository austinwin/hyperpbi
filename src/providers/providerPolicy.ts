import { EXTERNAL_PROVIDERS_AVAILABLE } from "./providerBuild";import { ProviderConfiguration } from "./providerTypes";
export interface ProviderPolicy { externalAvailable:boolean; certificationSafe:boolean; tilesAllowed:boolean; geocoderAllowed:boolean; warnings:string[]; }
export function resolveProviderPolicy(config:ProviderConfiguration|undefined,webAccessAvailable=EXTERNAL_PROVIDERS_AVAILABLE):ProviderPolicy{const externalAvailable=EXTERNAL_PROVIDERS_AVAILABLE&&webAccessAvailable;const tiles=Boolean(config?.basemap?.enabled&&config.basemap.provider!=="none");const geocoder=Boolean(config?.geocoder?.enabled&&config.geocoder.provider!=="none");const warnings:string[]=[];if((tiles||geocoder)&&!externalAvailable)warnings.push(EXTERNAL_PROVIDERS_AVAILABLE?"WebAccess is unavailable or denied by the Power BI host; external providers are disabled.":"External providers are unavailable in the core package. Use the maps package or disable providers.");if(geocoder&&!config?.privacyAcknowledged)warnings.push("Acknowledge the address-data privacy warning before geocoding.");return{externalAvailable,certificationSafe:!tiles&&!geocoder,tilesAllowed:externalAvailable&&tiles,geocoderAllowed:externalAvailable&&geocoder&&Boolean(config?.privacyAcknowledged),warnings};}
