import { GeocoderProvider } from "../providerTypes";
export const customGeocoderProvider:GeocoderProvider={id:"custom",label:"Custom endpoint",external:true,defaults:{provider:"custom",enabled:false,endpoint:"",rateLimitPerSecond:1,cache:true,autocomplete:false,userTriggeredOnly:true,resultLimit:1},async geocode(address,config,signal){if(!config.endpoint)throw new Error("Custom geocoder endpoint is missing.");const url=new URL(config.endpoint);url.searchParams.set("q",address);const response=await fetch(url.toString(),{signal,headers:{Accept:"application/json"}});if(!response.ok)throw new Error(`Geocoder HTTP ${response.status}`);const body=await response.json() as {lat?:number|string;lon?:number|string;latitude?:number|string;longitude?:number|string};const latitude=Number(body.latitude??body.lat),longitude=Number(body.longitude??body.lon);return Number.isFinite(latitude)&&Number.isFinite(longitude)?{latitude,longitude}:null;}};
