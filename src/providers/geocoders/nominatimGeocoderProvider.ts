import { GeocoderProvider } from "../providerTypes";
export const nominatimGeocoderProvider:GeocoderProvider={id:"nominatim",label:"Nominatim (OpenStreetMap)",external:true,defaults:{provider:"nominatim",enabled:true,endpoint:"https://nominatim.openstreetmap.org/search",rateLimitPerSecond:1,cache:true,autocomplete:false,userTriggeredOnly:true,resultLimit:1},async geocode(address,config,signal){const endpoint=config.endpoint??"https://nominatim.openstreetmap.org/search";const url=new URL(endpoint);url.searchParams.set("q",address);url.searchParams.set("format","jsonv2");url.searchParams.set("limit",String(Math.min(5,Math.max(1,config.resultLimit??1))));const response=await fetch(url.toString(),{signal,headers:{Accept:"application/json"}});if(!response.ok)throw new Error(`Geocoder HTTP ${response.status}`);const body=await response.json() as Array<{lat?:string;lon?:string}>;const first=body[0];const latitude=Number(first?.lat),longitude=Number(first?.lon);return Number.isFinite(latitude)&&Number.isFinite(longitude)?{latitude,longitude}:null;}};
