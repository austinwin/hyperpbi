import { customGeocoderProvider } from "./geocoders/customGeocoderProvider";import { noGeocoderProvider } from "./geocoders/noGeocoderProvider";import { nominatimGeocoderProvider } from "./geocoders/nominatimGeocoderProvider";import { arcgisGeocoderProvider } from "./geocoders/arcgisGeocoderProvider";import { GeocoderProvider,GeocoderProviderId } from "./providerTypes";const providers:Record<GeocoderProviderId,GeocoderProvider>={none:noGeocoderProvider,nominatim:nominatimGeocoderProvider,arcgis:arcgisGeocoderProvider,custom:customGeocoderProvider};export const listGeocoderProviders=()=>Object.values(providers);export const getGeocoderProvider=(id:GeocoderProviderId)=>providers[id]??providers.none;
