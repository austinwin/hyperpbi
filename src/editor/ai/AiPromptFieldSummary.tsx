import { NormalizedData } from "../../data/normalizeData";
import { createFieldAliasRegistry } from "../../fields/fieldAliasRegistry";
export function AiPromptFieldSummary({ data, onCopy }: { data: NormalizedData; onCopy: () => void }) { const registry=createFieldAliasRegistry(data);return <details class="hp-ai-details"><summary>{registry.entries.length} available field aliases</summary><div class="hp-field-chip-list">{registry.entries.map(field=><code title={`${field.displayName} · ${field.semanticRole} · ${field.kind}`}>{field.alias}</code>)}</div><button onClick={onCopy}>Copy field manifest</button></details>; }
