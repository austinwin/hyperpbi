import { NormalizedData } from "../data/normalizeData";
import { Diagnostic } from "../schema/diagnostics";
import { fieldManifestForPrompt } from "./fieldManifestForPrompt";
import { patternRegistry } from "../schema/patternRegistry";
export function buildRepairPrompt(json:string,diagnostics:Array<Diagnostic|string>,data:NormalizedData,warnings:string[]=[]):string{const structured:Diagnostic[]=diagnostics.map(item=>typeof item==="string"?{code:"INVALID_JSON",severity:"error",path:"/",message:item}:item);const componentTypes=Array.from(new Set(structured.map(item=>item.componentId).filter(Boolean)));return`Repair this HyperPBI version 2.0 specification. Correct only the diagnosed issues and preserve all valid unrelated content. Return one complete corrected JSON object only. Do not return Markdown fences, explanations, comments, or alternate versions.\n\nStructured diagnostics:\n${JSON.stringify(structured,null,2)}\n\nWarnings:\n${JSON.stringify(warnings,null,2)}\n\nValid field aliases:\n${JSON.stringify(fieldManifestForPrompt(data,[],"types"),null,2)}\n\nRelevant contract:\n- Root requires version \"2.0\" and components.\n- Every component requires a unique stable id and a listed component type.\n- Referenced fields must use a valid alias below.\n- Relevant component IDs: ${componentTypes.join(", ")||"See affected fragment"}.\n- Patterns: ${Object.keys(patternRegistry).join(", ")}.\n- No JavaScript, functions, credentials, unsafe HTML, comments, or unsupported properties.\n\nInvalid specification or affected fragment:\n${json}`;}
