import { componentPromptReference } from "../catalog/componentCatalog";
import { designPresets } from "../design/stylePresets";
import { dashboardRecipePrompt } from "./dashboardRecipes";

export const sharedComponentReference = `Shared version 2.0 component properties: type, id, dataset, title, subtitle, span, className, hidden, props, style, css, slots, data, visibility, interactions (safe event-specific actions), interaction (universal data behavior), uiAction (declarative interface behavior), variant, size, disabled, tooltip, ariaLabel, and icon. type and a globally unique stable id are required; interaction, interactions, and uiAction are optional and independent. Never generate deprecated component internal, external, selectable, or table selectionMode properties. id must match ^[A-Za-z][A-Za-z0-9_-]{0,99}$. span uses the responsive 12-column grid. Component css is parsed, allowlisted, and scoped beneath the component data-hp-id.`;
export const componentReference = `Component catalog (choose the smallest useful set; recommended components are preferred for normal dashboards):\n${componentPromptReference()}\n${sharedComponentReference}`;
export const appShellReference = `Application shell: Use root "app" for professional layout with navbar, sidebar, page header, footer, and navigation. App shell is declarative — do not rebuild it through custom HTML. Available: app.enabled, app.layout (vertical/horizontal), app.container (fluid/boxed), app.density (compact/normal), app.stickyHeader, app.contentPadding (none/compact/normal). brand.title, brand.subtitle, brand.icon, brand.shortTitle. navbar.visible, navbar.showSidebarToggle, navbar.showSearch, navbar.actions[], navbar.user (name, initials, status, menu[]). sidebar.visible, sidebar.width, sidebar.collapsedWidth, sidebar.collapsible, sidebar.navigation[]. sidebar.footer. pageHeader.visible, pageHeader.title, pageHeader.subtitle, pageHeader.breadcrumbs[], pageHeader.meta[], pageHeader.actions[]. footer.visible, footer.text.`;
export const uiActionReference = `UI Actions (separate from data interactions): clearFilters, setTab(target,value), setState(target,value), toggleState(target), toggleSidebar, openOverlay(target), closeOverlay(target), toggleOverlay(target), setStep(target,value), nextStep(target), previousStep(target), showToast(title,message,intent,durationMs), dismissToast(target), scrollTo(target), refresh. UI actions never trigger Power BI filtering. Data interaction executes separately. A component may have both uiAction and interaction.`;
export const overlayReference = `Overlay rules: Every modal, offcanvas, drawer, filterDrawer, dropdown, and popover requires an explicit unique ID. Use dropdown for commands, popover for contextual interactive content, offcanvas for persistent details or filters, and modal only for focused blocking tasks. All render through the root OverlayHost with safe declarative actions, focus management, Escape/outside dismissal, and viewport-aware positioning. Use uiAction openOverlay/closeOverlay/toggleOverlay and target an existing overlay ID. Legacy drawer and filterDrawer are normalized to offcanvas.`;
export const stylingReference = `Styling: use theme tokens for color, density, radius, padding, and gap. styles.globalCss is sanitized visual-wide CSS. styles.components contains reusable rules keyed by *, component type, or #component_id; local component css wins. Available presets: ${designPresets.map(item => item.name).join(", ")}. Keep a strong header → KPI → filter → chart → detail hierarchy. Avoid random bright colors, excessive gradients, huge empty cards, cluttered charts, fixed widths, horizontal overflow, and decorative whitespace.`;
export const chartReference = `Chart rules: Prefer first-class semantic charts: barChart, horizontalBarChart, lineChart, areaChart, pieChart, donutChart, scatterChart, gauge, heatmap, comboChart, waterfallChart, sankeyChart, treemapChart, funnelChart, or radarChart. Their options may change presentation but cannot replace generated dataset, series data, links, nodes, encode, transforms, series types, or series counts. Use advancedChart only for an uncommon ECharts configuration no semantic chart can express. advancedChart accepts sanitized JSON-only ECharts 6 options. String formatter templates are allowed; JavaScript functions, event-handler keys, external URLs, and executable markup are not. Use bounded labels, responsive grids, and restrained colors.`;
export const tableReference = `Table rules: use table for row detail and matrix for summarized pivots. Keep columns purposeful. Row clicks work whenever interaction.enabled is true; interaction.showSelector only shows checkbox/radio UI. Use internalMode:"highlight" to retain rows, "filter" to apply scoped HyperPBI filtering, or "none" to keep the source unchanged. Auto external mode selects exact row identities. Never guess a table or matrix filter field; externalMode:"filter" requires explicit interaction.field. Use pagination and bounded maxRows. Tabulator is not bundled; HyperPBI native table is the supported engine. Use density (compact/normal), striped, hover, showRowCount, pageSizeOptions, rowActions, and emptyState for better tables. Columns support sortable, resizable, visible, wrap, frozen (left/right), cellType (text/badge/progress), and intentMap.`;
export const mapReference = `Map rules: Generate Power BI spatial maps (lat/lon, geometry, addresses) by default. Location priority: Geometry > Lat/Lon > X/Y > Address. The practical public ArcGIS REST runtime supports arcgisFeature FeatureServer/MapServer layers, Power BI joins, viewport queries, service or configured renderers, service or configured labels, arcgisTile overlays, and basic arcgisDynamic images. Never invent service URLs, layer IDs, service fields, hosts, or tokens. Public HTTPS services only; no authentication, editing, 3D, relationship/tracing workflows, advanced collision, density grids, or non-4326 output. Center order is [latitude, longitude]. Use the Maps build profile for external requests and package restricted custom hosts via HYPERPBI_MAP_HOSTS.`;
export const interactionReference = `Universal interaction schema: interaction:{enabled,trigger:"auto"|"click"|"change",internalMode:"none"|"highlight"|"filter",internalScope:"self"|"others"|"all",externalMode:"none"|"auto"|"selection"|"filter",field,operator,value,selectionMode:"replace"|"toggle"|"add",multiSelect,showSelector,clearOnSecondClick}. Internal and external behavior are independent. Auto means controls/slicers filter Power BI and row/data-point/item clicks select exact identities. Use internalMode:"none" when the source must not react. External filters require an explicit unambiguous model-column field for table, matrix, map, scatter, and advanced chart interactions; never guess. Use selection for rows/points/features and filter for field-value slicers. Runtime Config is only a global external gate/fallback; component interaction wins. Custom slicers use repeat.distinctBy and interactions.onClick selectWhere/valueFromRow, then pass their payload through the universal engine. Never generate deprecated component internal, external, selectable, or selectionMode properties. Never invent externalSelection, selectionTarget, crossFilter, or powerBISelection in dashboard JSON.`;
export const calculationReference = `Calculation DSL operators: +, -, *, /, %, round, floor, ceil, abs, min, max, =, !=, >, >=, <, <=, and, or, not, concat, contains, startsWith, endsWith, lower, upper, trim, replace, dateDiff, dateAdd, year, quarter, month, week, day, today, now, coalesce, isNull, isNotNull, if, case. Expressions are typed JSON objects containing field/value/op; never functions. For new version 2.0 field pipelines, prefer logical-dataset derive and metrics because dataset static schemas expose those outputs to strict field validation. Root calculated metrics are available through metricGrid metric references and {{metric.key}} templates. Do not assume a root calculated-field key is a Field Manifest alias or named-dataset schema field.`;
export const securityReference = `Security: no JavaScript, eval, new Function, functions, inline event handlers, event-handler strings, scripts, iframes, external scripts, unsafe URLs, CSS imports, or executable markup. Safe ECharts string formatter templates are allowed, but function callbacks are not. HTML is DOMPurify-sanitized. CSS is parsed, allowlisted, and scoped. Advanced ECharts options are recursively sanitized.`;
export const recipeReference = `Dashboard recipes:\n${dashboardRecipePrompt()}`;
export const specShape = `{"version":"2.0","title":"Operations dashboard","theme":{"mode":"light","density":"compact"},"data":{"datasets":{"statusSummary":{"source":"powerbi","groupBy":["status"],"metrics":{"records":{"op":"count"},"totalAmount":{"op":"sum","field":"amount"}}}}},"definitions":{"detailTable":{"type":"table","columns":["recordId","status","amount"],"pagination":true,"pageSize":25}},"components":[{"type":"kpi","id":"record_count","title":"Records","dataset":"statusSummary","field":"records","aggregation":"sum"},{"type":"barChart","id":"amount_by_status","title":"Amount by status","dataset":"statusSummary","category":"status","measure":"totalAmount","interaction":{"enabled":true,"internalMode":"highlight","externalMode":"selection"}},{"use":"detailTable","type":"table","id":"record_details"}]}`;
