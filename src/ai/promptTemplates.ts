export const componentReference = `Supported components: grid, flex, split, leftPanel, toolbar, section, tabs, collapsible, accordion, searchBox, textInput, numberInput, slider, select, multiSelect, button, filterChips, kpi, metricGrid, infoCard, statusBadge, progressBar, alert, detailPanel, table, barChart, horizontalBarChart, lineChart, areaChart, pieChart, donutChart, scatterChart, gauge, heatmap, map, html, text, custom. Tabs should use tabs[].children. Global application styling uses styles.globalCss and reusable styles.components rules keyed by *, component type, or #component_id. Selectable tables filter to selected rows by default; use selectionMode "highlight" only when all rows should remain visible.`;
export const calculationReference = `Calculation DSL operators: +, -, *, /, %, round, floor, ceil, abs, min, max, =, !=, >, >=, <, <=, and, or, not, concat, contains, startsWith, endsWith, lower, upper, trim, replace, dateDiff, dateAdd, year, quarter, month, week, day, today, now, coalesce, isNull, isNotNull, if, case. Expressions are JSON objects containing field/value/op; never functions.`;
export const securityReference = `Security: no JavaScript, eval, functions, event-handler strings, scripts, iframes, external scripts, or unsafe URLs. HTML is sanitized. CSS is allowlisted and scoped to the component.`;
export const specShape = `{"version":"1.0","title":"Dashboard","styles":{"globalCss":".hp-card { border: 1px solid var(--hp-border); }","components":{"*":{"style":{"minWidth":0}}}},"calculations":{"fields":[],"metrics":[]},"components":[{"type":"metricGrid","id":"metrics","metrics":[{"title":"Records","aggregation":"count"}]},{"type":"table","id":"details","columns":["field_key"],"span":12}]}`;
