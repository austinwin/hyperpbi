export const networkGraphReference = `Network graph rules: use the first-class networkGraph component for relationships across entities already present in the selected Power BI/HyperPBI dataset. The public data contract is entities + relationships only; do not use sourceField/targetField edge-table mode and do not create or require a separate edge table. Each entities item requires a stable id and a supplied Field Manifest alias in field; label and labelField are optional. Each relationships item connects source and target entity ids, and optional branchLabel inserts a presentation-only grouping node such as Inspections or Work Orders. Power BI model relationships determine which values from different tables arrive together in each data row; networkGraph does not define joins or inspect model relationship metadata. HyperPBI deduplicates repeated entity and relationship combinations caused by flattened one-to-many branches, so a normal model such as Service Request -> Incident -> Inspection and Incident -> Work Order can be authored directly from the bound fields. Any number of entity definitions, relationship definitions, branches, and hierarchy levels are supported subject to the current data view and maxNodes safety bound. Prefer hybrid for operational/process trees because it derives hierarchy levels, reduces crossings, settles spacing, and then renders fixed positions without continuous physics. Use hierarchical when completely deterministic level ordering is preferred, force only when the user explicitly wants an exploratory free-form network, and circular for compact peer networks. Hybrid/hierarchical spacing can use levelGap and nodeGap; visual edge styling can use edgeWidth, edgeOpacity, edgeCurvature, and arrowSize. Keep maxNodes bounded and keep very large models filtered to a useful root/context because Power BI can still materialize repeated row combinations before HyperPBI deduplicates them. Entity-node clicks preserve the entity field and contributing Power BI row lineage; externalMode:"selection" is the safe default. Never invent entity fields, relationship joins, JavaScript callbacks, or network data sources, and prefer networkGraph over advancedChart when this contract fits.`;
