export const networkGraphReference = `Network graph rules: use the first-class networkGraph component for node-link relationships, dependency networks, lineage views, force graphs, relationship explorers, and effort trees when the selected dataset can express each edge with sourceField and targetField. Optional sourceLabelField/targetLabelField, sourceCategoryField/targetCategoryField, edgeLabelField, and numeric edgeWeightField refine presentation. layout is force, circular, or hierarchical; prefer force for exploratory networks and hierarchical for directional operational trees. Keep maxNodes bounded. Node and edge clicks preserve contributing Power BI row lineage, so externalMode:"selection" is the safe default. Do not guess an external filter field when a node can appear as both source and target. Never simulate this with advancedChart when networkGraph fits, and never emit JavaScript callbacks or network data sources.`;
