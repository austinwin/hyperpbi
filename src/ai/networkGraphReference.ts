export const networkGraphReference = `Network graph rules: use the first-class networkGraph component for node-link relationships, dependency networks, lineage views, relationship explorers, and effort trees when the selected dataset can express each edge with sourceField and targetField. Optional sourceLabelField/targetLabelField, sourceCategoryField/targetCategoryField, edgeLabelField, and numeric edgeWeightField refine presentation. layout is hybrid, hierarchical, force, or circular. Prefer hybrid for operational/process trees because it derives hierarchy levels, reduces crossings, settles spacing, and then renders fixed positions without continuous physics. Use hierarchical when completely deterministic level ordering is preferred, force only when the user explicitly wants an exploratory free-form network, and circular for compact peer networks. Hybrid/hierarchical spacing can use levelGap and nodeGap; visual edge styling can use edgeWidth, edgeOpacity, edgeCurveness, and arrowSize. Keep maxNodes bounded. Node and edge clicks preserve contributing Power BI row lineage, so externalMode:"selection" is the safe default. Do not guess an external filter field when a node can appear as both source and target. When the business model has separate one-to-many entity tables (for example Service Request, Incident, Inspection, and Work Order), do not bind all child tables directly into one Power BI visual because that can create Cartesian row multiplication. Keep those normalized source tables and prepare a small derived relationship/edge dataset with source ID, target ID, and optional source/target type/label columns for networkGraph. HyperPBI does not independently join arbitrary model tables. Never simulate this with advancedChart when networkGraph fits, and never emit JavaScript callbacks or network data sources.`;
