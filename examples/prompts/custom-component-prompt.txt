Create one complete HyperPBI 2.0 dashboard with a safe custom summary component, a semantic chart, and a selectable native detail table. Use only aliases supplied in the Field Manifest. Every component must have a stable unique ID. The custom component may use sanitized HTML, slots, bounded repeat, template bindings, scoped CSS, and allowlisted selectWhere/clearSelection actions; prefer a first-class component if it fits. Do not emit JavaScript, callbacks, inline handlers, credentials, or invented fields. Return one valid JSON object only, without Markdown or explanation.
