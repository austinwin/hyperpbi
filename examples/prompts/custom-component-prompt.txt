Create a compact HyperPBI dashboard with one safe custom summary component, a useful chart, and a selectable detail table. The custom component may use sanitized html, props, slots, repeat, template bindings, scoped css, and predefined selectWhere/clearSelection actions. It must not contain JavaScript or inline event handlers. Use only supplied normalized field keys. Return valid JSON only without markdown or explanation.
