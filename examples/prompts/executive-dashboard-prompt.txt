Create a compact executive operations dashboard in HyperPBI. Use only the normalized field keys supplied below. Include a record count, two decision-oriented metrics, one category comparison chart, a searchable selectable detail table, and external Power BI interactions. Return one valid JSON object only. Do not use markdown, comments, JavaScript, functions, event handlers, or invented fields. Use calculation DSL objects for derived logic and component-scoped CSS only when needed.
